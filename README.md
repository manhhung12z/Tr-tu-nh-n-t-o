secure_filename #Thư viện này dùng để làm sạch tên file (xóa ký tự đặc biệt, dấu tiếng Việt, khoảng trắng)
#sau khi trình duyệt upload ảnh lên yolo ko thể xử lý được trực tiếp file trên cần thư viện uploads để lưu file tạm thời.
#có các thư viện để sử dụng như sử dụng os để tạo thư mục uploads/ tự động nếu chưa tồn tại bằng os.makedis().
